===============================================
Private Rideshare - 手动上传部署说明
===============================================

✅ 已修复的问题：
1. 移除了 Next.js 文件的干扰
2. 配置 vercel.json 禁用自动构建
3. 配置 .vercelignore 只部署静态文件

📦 需要上传的文件（仅这3个文件）：
--------------------------------------------
1. index.html          - 主HTML文件
2. script.js           - JavaScript文件
3. public/favicon.ico  - 网站图标

⚙️ 需要上传的配置文件：
--------------------------------------------
1. vercel.json         - Vercel配置（禁用构建）
2. .vercelignore       - 忽略其他所有文件

🚀 手动上传步骤：
--------------------------------------------

方法1: 通过 Vercel Dashboard 上传
1. 登录 https://vercel.com/dashboard
2. 选择项目 "ride-share-six"
3. 进入 Settings -> General
4. 在 "Build & Development Settings" 中：
   - Framework Preset: Other
   - Build Command: 留空或删除
   - Output Directory: .
   - Install Command: 留空或删除
5. 保存设置
6. 返回项目页面，点击 "Redeploy"

方法2: 创建临时部署目录
1. 创建新文件夹: rideshare-static
2. 只复制这些文件到新文件夹:
   - index.html
   - script.js
   - vercel.json
   - .vercelignore
   - public/favicon.ico
3. 将 rideshare-static 文件夹上传到 GitHub 新仓库
4. 在 Vercel 中导入这个新仓库

方法3: 使用 Git 但只推送必要文件
1. 在当前目录执行:
   git add index.html script.js vercel.json .vercelignore public/favicon.ico
   git commit -m "Deploy static version only"
   git push

🔍 验证部署：
--------------------------------------------
部署完成后访问: https://ride-share-six.vercel.app/

应该看到:
✅ 页面正常加载，没有404错误
✅ 控制台没有 "MIME type" 错误
✅ script.js 正确加载
✅ 可以点击 "Connect Wallet" 按钮

❌ 如果仍然有问题：
--------------------------------------------
1. 确保 Vercel 没有检测为 Next.js 项目
   - 在 Project Settings -> General 中查看
   - Framework Preset 应该是 "Other" 而不是 "Next.js"

2. 清除 Vercel 缓存
   - 在 Deployments 页面
   - 点击最新部署的 "..." 菜单
   - 选择 "Redeploy" 并勾选 "Clear cache"

3. 检查部署的文件
   - 在部署详情页面，查看 "Source" 标签
   - 应该只看到 index.html, script.js, vercel.json
   - 不应该看到 app/, .next/, node_modules/ 等

📋 关键配置说明：
--------------------------------------------

vercel.json:
- buildCommand: null     (禁用构建)
- installCommand: null   (禁用依赖安装)
- outputDirectory: "."   (当前目录)

.vercelignore:
- 忽略所有文件 (*)
- 只允许 index.html, script.js, public/
- 明确忽略 app/, .next/, node_modules/ 等

🎯 部署后将只包含：
--------------------------------------------
/
├── index.html
├── script.js
└── public/
    └── favicon.ico

其他所有文件都会被忽略！

===============================================
准备完成！您现在可以手动上传这些文件了。
===============================================
